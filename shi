local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("K13 MUSCLE MASTERS 🔥", "DarkTheme")

local Farm = Window:NewTab("Farm")
local FarmSection = Farm:NewSection("Main Farming")

FarmSection:NewToggle("Auto Farm (Turbo)", "Ultra fast clicking and reps", function(state)
    getgenv().AutoFarm = state
    if state then
        task.spawn(function()
            while getgenv().AutoFarm do
                local remote = game:GetService("Players").LocalPlayer:FindFirstChild("muscleEvent") or (game:GetService("ReplicatedStorage"):FindFirstChild("rEvents") and game:GetService("ReplicatedStorage").rEvents:FindFirstChild("repRemote"))
                if remote then
                    if remote.ClassName == "RemoteEvent" then
                        remote:FireServer("rep")
                    elseif remote.ClassName == "RemoteFunction" then
                        remote:InvokeServer("rep")
                    end
                end
                game:GetService("VirtualInputManager"):SendMouseButtonEvent(0, 0, 0, true, game, 0)
                game:GetService("VirtualInputManager"):SendMouseButtonEvent(0, 0, 0, false, game, 0)
                task.wait() 
            end
        end)
    end
end)

FarmSection:NewToggle("Auto Rebirth", "Automatic rebirth", function(state)
    getgenv().AutoRebirth = state
    task.spawn(function()
        while getgenv().AutoRebirth do
            pcall(function()
                game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
            end)
            task.wait(0.1)
        end
    end)
end)

local Misc = Window:NewTab("Misc")
local MiscSection = Misc:NewSection("Eggs & Utilities")

MiscSection:NewDropdown("Select Eggs", "Choose Abyss Egg for Titan", {"Abyss Egg", "Common Egg", "Rare Egg"}, function(v)
    getgenv().SelectedEgg = v
end)

MiscSection:NewToggle("Auto Hatch", "Hatch selected egg", function(state)
    getgenv().AutoHatch = state
    task.spawn(function()
        while getgenv().AutoHatch do
            local egg = getgenv().SelectedEgg or "Abyss Egg"
            pcall(function()
                game:GetService("ReplicatedStorage").rEvents.petEvent:FireServer("openEgg", egg, "triple")
            end)
            task.wait(0.01)
        end
    end)
end)

MiscSection:NewToggle("Auto Spin", "Auto spin wheel", function(state)
    getgenv().AutoSpin = state
    task.spawn(function()
        while getgenv().AutoSpin do
            pcall(function()
                game:GetService("ReplicatedStorage").rEvents.wheelRemote:InvokeServer("spinWheel")
            end)
            task.wait(1)
        end
    end)
end)

MiscSection:NewToggle("Anti AFK", "No kick", function(state)
    getgenv().AntiAFK = state
end)

game:GetService("Players").LocalPlayer.Idled:Connect(function()
    if getgenv().AntiAFK then
        game:GetService("VirtualUser"):CaptureController()
        game:GetService("VirtualUser"):ClickButton2(Vector2.new())
    end
end)

local Settings = Window:NewTab("Settings")
local SettingsSection = Settings:NewSection("UI Controls")

SettingsSection:NewLabel("Press 'RightControl' to Hide/Show UI")

SettingsSection:NewKeybind("Toggle Key", "Change hide/show key", Enum.KeyCode.RightControl, function()
	Library:ToggleUI()
end)
