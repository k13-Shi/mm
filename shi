local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("K13 MUSCLE MASTERS 🔥", "GrapeTheme")

-- [ TAB 1: HOME ]
local Home = Window:NewTab("K13 Home")
Home:NewSection("🟣 K13 PREMIUM VIOLET 🟣")
Home:NewSection("STATUS: ALL FEATURES FIXED")

-- [ TAB 2: FARM ]
local Farm = Window:NewTab("Farm")
local FarmSection = Farm:NewSection("Turbo Farming")

FarmSection:NewToggle("8-PACK SPEED (STABLE)", "Ultra Fast Reps + Auto Equip", function(state)
    getgenv().AutoFarm = state
    task.spawn(function()
        while getgenv().AutoFarm do
            local LP = game.Players.LocalPlayer
            local tool = LP.Backpack:FindFirstChildOfClass("Tool") or LP.Character:FindFirstChildOfClass("Tool")
            if tool and LP.Character then LP.Character.Humanoid:EquipTool(tool) end
            local Remote = LP:FindFirstChild("muscleEvent") or (game:GetService("ReplicatedStorage"):FindFirstChild("rEvents") and game:GetService("ReplicatedStorage").rEvents:FindFirstChild("repRemote"))
            if Remote then for i = 1, 25 do pcall(function() Remote:FireServer("rep") end) end end
            task.wait(0.001)
        end
    end)
end)

FarmSection:NewToggle("Instant Auto Rebirth", "No delay", function(state)
    getgenv().AutoRebirth = state
    task.spawn(function()
        while getgenv().AutoRebirth do
            pcall(function() game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest") end)
            task.wait(0.1)
        end
    end)
end)

-- [ TAB 3: COMBAT ]
local Combat = Window:NewTab("Combat")
local CombatSection = Combat:NewSection("Kill & View (No TP)")

local function forceEquipPunch()
    local LP = game.Players.LocalPlayer
    local punch = LP.Backpack:FindFirstChild("Punch") or LP.Character:FindFirstChild("Punch")
    if punch and LP.Character and punch.Parent ~= LP.Character then
        LP.Character.Humanoid:EquipTool(punch)
    end
end

CombatSection:NewToggle("💀 KILL ALL (NO TELEPORT)", "Instant kill everyone", function(state)
    getgenv().KillAll = state
    task.spawn(function()
        while getgenv().KillAll do
            forceEquipPunch()
            for _, v in pairs(game.Players:GetPlayers()) do
                if v ~= game.Players.LocalPlayer and v.Character then
                    pcall(function()
                        game.Players.LocalPlayer.muscleEvent:FireServer("punchClick", v.Character)
                        game.Players.LocalPlayer.muscleEvent:FireServer("rep", v.Character)
                    end)
                end
            end
            task.wait(0.1)
        end
    end)
end)

CombatSection:NewToggle("☣️ Auto Toxic", "Chat L on kill", function(state)
    getgenv().AutoToxic = state
end)

-- [ TAB 4: AUTO HATCH ]
local Pets = Window:NewTab("Auto Hatch")
local PetSection = Pets:NewSection("Egg Hatching")
PetSection:NewDropdown("Select Egg", "List", {"Abyss Egg", "Legendary Egg", "Epic Egg", "Rare Egg", "Common Egg"}, function(v)
    getgenv().SelectedEgg = v
end)

PetSection:NewToggle("Start Auto Hatch (Triple)", "Hatch instantly", function(state)
    getgenv().AutoHatch = state
    task.spawn(function()
        while getgenv().AutoHatch do
            local egg = getgenv().SelectedEgg or "Abyss Egg"
            pcall(function() game:GetService("ReplicatedStorage").rEvents.petEvent:FireServer("openEgg", egg, "triple") end)
            task.wait(0.1)
        end
    end)
end)

-- [ ANTI-AFK MINI NOTIF SYSTEM ]
local CoreGui = game:GetService("CoreGui")
local NotifGui = Instance.new("ScreenGui", CoreGui)
NotifGui.Name = "K13_AFK_Mini"

local AfkButton = Instance.new("ImageButton", NotifGui)
AfkButton.Name = "AFK_Indicator"
AfkButton.Size = UDim2.new(0, 45, 0, 45)
AfkButton.Position = UDim2.new(0, 15, 0.5, 70) -- Sa ilalim ng main button
AfkButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
AfkButton.Image = "rbxassetid://6031097225"
AfkButton.ImageColor3 = Color3.fromRGB(255, 255, 255)
AfkButton.Draggable = true
Instance.new("UICorner", AfkButton).CornerRadius = UDim.new(1, 0) -- Perfect Circle

local Stroke = Instance.new("UIStroke", AfkButton)
Stroke.Color = Color3.fromRGB(138, 43, 226)
Stroke.Thickness = 2
Stroke.Enabled = false -- Mag-e-enable lang pag active

Farm:NewSection("Safety"):NewToggle("Anti-AFK (No Kick)", "Stay online 24/7", function(state)
    getgenv().AntiAFK = state
    Stroke.Enabled = state
    AfkButton.ImageColor3 = state and Color3.fromRGB(138, 43, 226) or Color3.fromRGB(255, 255, 255)
end)

-- [ MAIN K13 LOGO BUTTON ]
if CoreGui:FindFirstChild("K13_Main_Logo") then CoreGui:FindFirstChild("K13_Main_Logo"):Destroy() end
local MainGui = Instance.new("ScreenGui", CoreGui)
MainGui.Name = "K13_Main_Logo"

local MainButton = Instance.new("ImageButton", MainGui)
MainButton.Size = UDim2.new(0, 60, 0, 60)
MainButton.Position = UDim2.new(0, 15, 0.5, -30)
MainButton.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
MainButton.Image = "rbxassetid://6031097225" 
MainButton.Draggable = true
Instance.new("UICorner", MainButton).CornerRadius = UDim.new(1, 0) -- Perfect Circle

MainButton.MouseButton1Click:Connect(function() Library:ToggleUI() end)

-- [ SETTINGS ]
local Settings = Window:NewTab("Settings")
Settings:NewSection("Controls"):NewKeybind("Toggle UI", "Key: RightControl", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
end)

-- [ AFK LOGIC ]
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    if getgenv().AntiAFK then
        game:GetService("VirtualUser"):CaptureController()
        game:GetService("VirtualUser"):ClickButton2(Vector2.new())
    end
end)
