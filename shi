local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("K13 MUSCLE MASTERS 🔥", "DarkTheme")

-- MAIN TAB (K13 HOME)
local Main = Window:NewTab("K13 Home")
local MainSection = Main:NewSection("🟣 K13 PREMIUM VIOLET 🟣")
MainSection:NewLabel("STATUS: GOD-SPEED LOADED")

-- FARM TAB (8-PACK SPEED)
local Farm = Window:NewTab("Farm")
local FarmSection = Farm:NewSection("Turbo Farming")

FarmSection:NewToggle("Fast Farm", "Pinakamabilis na Reps (Stay Mode)", function(state)
    getgenv().AutoFarm = state
    if state then
        task.spawn(function()
            while getgenv().AutoFarm do
                local LP = game.Players.LocalPlayer
                local Remote = LP:FindFirstChild("muscleEvent") or (game:GetService("ReplicatedStorage"):FindFirstChild("rEvents") and game:GetService("ReplicatedStorage").rEvents:FindFirstChild("repRemote"))
                if Remote then
                    for i = 1, 15 do -- 15 Reps sabay-sabay bawat loop
                        pcall(function()
                            Remote:FireServer("rep")
                        end)
                    end
                end
                game:GetService("VirtualInputManager"):SendMouseButtonEvent(0, 0, 0, true, game, 0)
                game:GetService("VirtualInputManager"):SendMouseButtonEvent(0, 0, 0, false, game, 0)
                task.wait(0.001)
            end
        end)
    end
end)

FarmSection:NewToggle("Instant Rebirth", "Auto Rebirth", function(state)
    getgenv().AutoRebirth = state
    task.spawn(function()
        while getgenv().AutoRebirth do
            game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
            task.wait(0.1)
        end
    end)
end)

FarmSection:NewToggle("Anti-AFK + Timer", "No Kick", function(state)
    getgenv().AntiAFK = state
    _G.AfkFrame.Visible = state
end)

local CoreGui = game:GetService("CoreGui")
if CoreGui:FindFirstChild("K13_Violet_Logo") then CoreGui:FindFirstChild("K13_Violet_Logo"):Destroy() end

local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Name = "K13_Violet_Logo"
ScreenGui.Parent = CoreGui

ImageButton.Name = "K13_Floating_Button"
ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
ImageButton.Position = UDim2.new(0, 15, 0.5, 0)
ImageButton.Size = UDim2.new(0, 60, 0, 60)
ImageButton.Image = "rbxassetid://6031097225" -- Ninja/Shield Logo
ImageButton.Draggable = true

UICorner.CornerRadius = UDim.new(0, 15)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Click:Connect(function()
    Library:ToggleUI()
end)

local Settings = Window:NewTab("Settings")
Settings:NewSection("UI Controls"):NewKeybind("Toggle UI", "RightControl", Enum.KeyCode.RightControl, function()
    Library:ToggleUI()
end)
